# Deployment requirements - MINIMAL for Railway
# Removed heavy causal packages that cause dependency conflicts
# These packages fail gracefully at runtime if missing

# Core API (required)
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
pydantic==2.10.4
pydantic-settings==2.7.1

# Data Processing - use compatible versions
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1

# Causal Inference - ONLY the essentials that work
statsmodels==0.14.4
scikit-learn==1.6.0

# REMOVED heavy causal packages - they cause dependency hell
# dowhy, causal-learn, pgmpy, linearmodels, pyitlib
# The code handles missing packages gracefully

# Kafka / Confluent
confluent-kafka==2.6.1
fastavro==1.9.7

# Google AI
protobuf==5.29.2
google-cloud-aiplatform==1.74.0

# API Clients
praw==7.8.1
requests==2.32.3
aiohttp==3.11.11
httpx==0.28.1
authlib==1.4.1

# Configuration & Utilities
python-dotenv==1.0.1
loguru==0.7.3
tenacity==9.0.0

# Agent Framework - use older compatible versions
langgraph==0.2.60
langchain==0.3.14
langchain-core==0.3.30
langchain-google-genai==2.0.8
langchain-openai==0.3.0
