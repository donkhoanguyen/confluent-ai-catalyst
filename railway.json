{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "cd cryptosentinel && pip install -r requirements.txt"
    },
    "deploy": {
      "startCommand": "cd cryptosentinel && python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }